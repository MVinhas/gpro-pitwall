# SFTP deploy target for the local-only deploy tooling (bin/deploy.sh, gitignored).
# Copy to .deploy.env (gitignored) and fill in. Never commit real values.

SFTP_HOST=
SFTP_USER=
SFTP_PASS=
#SFTP_PORT=22
# Remote directory that will contain the gpro-pitwall/ app folder.
SFTP_REMOTE_DIR=
